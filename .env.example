# Environment variables for Heroku deployment

# Database path (SQLite file location)
DATABASE_URL=./appointments.db

# Node environment
NODE_ENV=production

# Port (automatically set by Heroku, but can be overridden)
PORT=3000

# API configuration
API_URL=/api

# Enable CORS
CORS_ORIGIN=*
